# pipeline/lib/ — support modules imported by pipeline steps at runtime.
# These are NOT pipeline steps themselves. They hold reference data
# (countries used by 4_merge.py for country normalization).
